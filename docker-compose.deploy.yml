services:
  kanban:
    ports: !override
      - "43327:3000"
    volumes: !override
      - /home/roomhacker/todo-kanban/tasks:/app/data/tasks
    environment:
      TASKS_DIR: /app/data/tasks
      NODE_ENV: production
